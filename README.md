Installation
1. Drop the zip or folder into your world's datapacks directory.
2. Start or reload the world. Use /reload if the world is already loaded.
3. Only newly generated chunks will use the new placement rules.

Quick tests
- Generate new chunks and use: /locate structure integrated_stronghold:stronghold
- Note: If BigGlobe/Sinytra still forces surface placement, place the dispatcher JSON into the exact compat template BigGlobe expects (I can adapt it 1:1 if you paste that template).

AI generation warning
Note: This datapack was generated with assistance from an AI tool. Please review and test the files before use; changes may be required for your exact modpack or server setup.
