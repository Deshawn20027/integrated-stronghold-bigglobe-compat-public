{
  "type": "script",
  "dispatcher": [
    "; BigGlobe script dispatcher: place Integrated Stronghold at random Y between -600 and -400",
    "int y = random.nextInt(201) - 600",
    "int x = (minModifiableX + maxModifiableX) / 2",
    "int z = (minModifiableZ + maxModifiableZ) / 2",
    "; Use the StructureTemplate constant factory and the exposed placeStructureTemplate function",
    "placeStructureTemplate(x, y, z, StructureTemplate('integrated_stronghold:stronghold'))"
  ]
}
